apiVersion: v1
kind: Service
metadata:
  name: registration-app-service
  labels:
    app: registration-app-aws
spec:
  selector:
    app: registration-app-aws
    
  ports:
  - port: 80
    targetPort: 8082

  type: LoadBalancer
